// Core traits and types for Lithify.
// Zero external dependencies — this crate defines all shared interfaces.

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
